# Mobile Release Evidence: 2026-09-26

**Status**: Local automated gates PASS. Device, iOS and CI-run gates NOT RUN (reasons below).
Base commit `67e478b`; results are for the working tree on branch `Kashyep/flutter-mobile-client`.
Host: Windows 11 (10.0.26200), Flutter 3.47.5 (Dart 3.13.4, framework `6a19cca564`), JDK 17.

Baseline before this work (`67e478b`): `flutter pub get` OK; `flutter analyze` 8 issues (6
`constant_identifier_names` on `Outcome`, `unreachable_switch_default` in chat, missing
`assets/google_fonts/` directory); `mobile/test/` empty.

## §8 gates

| Gate | Check | Result | Observation |
| --- | --- | --- | --- |
| Typecheck / analyze | `flutter analyze` | **PASS** | "No issues found!" (Windows and Linux container) |
| Format / lint | `dart format --output=none --set-exit-if-changed .` | **PASS** | 39 files, 0 changed, exit 0 |
| Tests | `flutter test --exclude-tags golden` | **PASS** | 183 passed (Windows); 183 passed (Linux container) |
| Goldens | `flutter test --tags golden` | **PASS** | 74 passed: 9 screens/states × light/dark × 1.0/2.0 × glass/opaque + 2 glass-legibility. Windows baselines `test/goldens/windows/`, Linux baselines `test/goldens/linux/` (generated and re-verified in `ubuntu:24.04` + official Flutter 3.47.5 Linux archive). Full `flutter test`: 257 passed on both |
| Build (Android) | `flutter build appbundle --release` | **PASS** | `app-release.aab` 51.6 MB; manifest package `ai.karmi.app`; no `usesCleartextTraffic` in release. Signed with the debug key (no release keystore configured) |
| Build (iOS) | `flutter build ipa --release --no-codesign` | **NOT RUN** | Windows host cannot build iOS. CI job `mobile-ios-build` (macos-latest) added but its run was not observed |
| CI | `.github/workflows/ci.yml` | **NOT RUN** | Jobs defined (MOB-012); not pushed/observed from this session |
| Contrast | Token unit test + glass legibility golden | **PASS (automated)** | `karmi_colors_test.dart` recomputes all 38 §2.4 ratios (±0.01) and pins pass/fail; `textContrastGuideline` passes on every screen test and on glass over pure white / `#08160a`. Limitation: in `flutter test` the glass shader cannot load, so the legibility check runs on the non-shader fallback; real-shader legibility NOT RUN on device |
| Screen reader (automated) | `meetsGuideline` android/iOS tap target, labeled tap target, text contrast | **PASS** | On every chat state and every Phase 4 screen, incl. text scale 2.0 |
| Text scaling (automated) | `textScaler` 2.0 | **PASS** | No overflow on Chat (glass + opaque), Tasks, Usage, Memory, Settings, Welcome, Plans. Note: Material `AppBar`/`NavigationBar` and `GlassTabBar` labels do not grow with text scale (framework/package behaviour: clamp or scale-down), visible in the x2.0 goldens |
| Reduced motion / transparency (automated) | Widget tests | **PASS** | `glass_motion_test.dart`: transitions `Duration.zero`, static loader, opaque fallback, high contrast → foreground borders + 3px focus ring |
| Screen reader (manual) | TalkBack on Pixel 9a | **NOT RUN** | Pixel 9a disconnected during the session (adb: no devices; not present in Windows PnP). Owner chose to skip device checks |
| Screen reader (manual) | VoiceOver | **NOT RUN** | No macOS host / iOS device |
| Focus / keyboard (manual) | Hardware keyboard on Pixel 9a / iPad | **NOT RUN** | Device disconnected (Pixel); no iOS device. Automated: focused button paints the 2px/3px ring |
| Text scaling (manual) | OS font size max | **NOT RUN** | Device disconnected |
| Reduced motion / transparency (manual) | OS toggles | **NOT RUN** | Device disconnected; iOS unavailable. Android auto-bridge of high contrast into glass remains UNVERIFIED |
| Performance | TTFF (`flutter run --profile --trace-startup`), input-to-frame | **NOT RUN** | Device disconnected. No reference low-end Android device named (Q8); Pixel 9a is not low-end |
| Glass frame budget | DevTools overlay while scrolling Chat | **NOT RUN** | Device disconnected |
| shdr-01 cost | n/a | **N/A** | shdr-01 omitted (licence); T4.8 hero shader skipped (not requested) |

## Backend change (API-001, `GET /v1/tasks`)

| Gate | Result |
| --- | --- |
| `python scripts/tasks.py lint` | PASS (ruff + secret scan) |
| `python scripts/tasks.py typecheck` | PASS (mypy strict, 17 files) |
| `python scripts/tasks.py test-unit` | PASS (32 passed) |
| `python scripts/tasks.py test-e2e` | PASS (15 passed, incl. `test_task_list_is_account_scoped`) |
| `python scripts/tasks.py test-integration` | NOT RUN (read-only query; no money/concurrency path) |
| Live dev server smoke | PASS: `POST /dev/token` then `GET /v1/tasks` → `[]`, `GET /v1/usage` → synthetic Ananta usage |

## Earlier device observation (before this session's changes)

Memory.md records a debug APK of the first-pass client (package `ai.karmi.karmi_app`) installed and
launched on the Pixel 9a with Impeller/Vulkan and liquid_glass active. That build predates this
work and is not evidence for the current client.
