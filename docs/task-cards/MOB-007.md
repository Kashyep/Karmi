# MOB-007 (T4.3): Settings

**Status**: Done
**Files**: `mobile/lib/features/settings/settings_screen.dart`

- [x] Theme control: three buttons with text + icon (System / Light / Dark; replaced the SegmentedButton in MOB-014 so each choice gets its own focus ring and selected state),
  inside the glass settings header (the only glass surface on the screen). Persisted.
- [x] Reduce transparency switch (persisted; forced on by platform high contrast).
- [x] Licences entry → `showLicensePage` (lists the five OFL font entries).
- [x] Sign out clears the stored token and returns to Welcome.

## Verification
`test/features/screens_test.dart` group "T4.3 Settings"; `test/theme/glass_motion_test.dart`
(toggle); a11y guidelines; text scale 2.0; goldens `settings_*`.
