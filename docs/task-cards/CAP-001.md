# CAP-001 — Runtime capability verification

- Requirement: Agent_Team capability test
- Base commit: pre-repository workspace
- Owners: Boss, Supervisor, capability worker
- Scope: read-only runtime and specification review
- Requested configurations: Boss `gpt-5.6-sol/high`; Supervisor `gpt-5.6-terra/medium`; worker `gpt-5.6-luna/medium`
- Acceptance: genuine child tasks created; configured/requested/observed settings distinguished

Result: child tasks were created through native orchestration controls with explicit requested
model and reasoning values. Neither child could observe independent runtime model metadata.
Boss effective model metadata was also unavailable. Mixed-role orchestration is therefore
observed; effective model identity remains unknown and is not inferred from prose.

Supervisor risk: WhatsApp is blocked for the provisional India launch and the consumer web app
must be promoted explicitly to avoid a release with no customer channel.

Worker risk: atomic reservation and unknown-provider-cost reconciliation are hard correctness
gates under concurrency.

