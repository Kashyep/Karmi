# MOB-006 (T4.2): Memory

**Status**: Done
**Files**: `mobile/lib/features/memory/memory_screen.dart`

- [x] Real `GET /v1/notes`, `POST /v1/notes` (with an idempotency key reused until the text
  changes or the save succeeds), `DELETE /v1/notes/{id}`.
- [x] Loading, empty (Trykker), error + Retry; add-note input; confirm dialog before deleting;
  "Note deleted" confirmation; failed save/delete keeps state and explains.
- Notes are opaque cards. (A SnackBar was not usable: `GlassScaffold` is not a Material
  `Scaffold`, which the test caught.)

## Verification
`test/features/screens_test.dart` group "T4.2 Memory" (list, empty, error→retry, add, delete
cancel/confirm, failed delete); a11y guidelines; text scale 2.0; goldens `memory_*`.
