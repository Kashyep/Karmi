# MOB-011 (T4.7): Task confirmation sheet (stub)

**Status**: Done as a stub — backend gap recorded
**Files**: `mobile/lib/features/tasks/task_confirmation_sheet.dart`

- [x] "Review details" on `ASK_USER` replies opens a modal sheet with an opaque `--card` body.
- [x] Shows the reply verbatim and Action / Target / Date and time / Timezone as "Not provided",
  a "Missing details" banner, Close, and a disabled Confirm.

**Gap (backend)**: `MessageView` has no structured action payload (action, target, due time,
timezone, expiry). Nothing can be confirmed safely until a backend task card adds one; Design.md
states "Missing details / expired confirmation / already completed" remain unimplemented.

## Verification
`test/features/chat_test.dart` "ASK_USER opens the opaque confirmation sheet" (opaque body,
disabled Confirm, a11y guidelines); goldens `confirmation_sheet_*`.
