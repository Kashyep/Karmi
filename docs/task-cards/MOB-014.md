# MOB-014: Fixes for failures found in the Pixel 9a release gates

**Status**: Done (uncommitted working tree; not yet through CI)
**Found by**: MOB-013 device run, 2026-09-26
**Rule followed**: no app code changed without a failing gate; each fix was preceded by a test that
reproduced the failure (confirmed failing before the fix, passing after).

| Failure | Reproducing test | Fix (files) |
| --- | --- | --- |
| Enabled send icon invisible in glass shell (GlassScaffold's Cupertino `IconTheme` made the icon primary-on-primary) | `test/features/device_regressions_test.dart` | `lib/shell/karmi_shell.dart`: restore Material `IconTheme` for the body |
| Glass app bar had no platter; title illegible over scrolled content | same file (light + dark) | `lib/theme/karmi_glass.dart` `KarmiGlass.tint`; app bar background |
| Reduce transparency / contrast toggle erased the chat transcript | same file | `GlobalKey` on the shell `IndexedStack` |
| Settings Licences semantics node covered its card (TalkBack order wrong) | same file | `lib/widgets/karmi_card.dart` `semanticContainer`; Settings passes `false` |
| Glass overflow menu items absent from the accessibility tree | same file (checks the real semantics tree) | Material `PopupMenuButton` in both shells; `GlassMenu` removed |
| No visible keyboard focus (switch, tiles, glass tabs, theme segments, filled button); Android stays in touch highlight mode | `test/features/keyboard_focus_test.dart` | `lib/widgets/focus_ring.dart` (`KarmiFocusRing`); theme choice as three buttons in `settings_screen.dart`; filled-button focus ring in `karmi_theme.dart` |
| Theme choice lost "selected" state (regression from the previous fix) | device regressions test | `Semantics(selected:)` inside each theme button |

## Verification
- `flutter test`: 273 passed (Windows). The Linux container passed the same suite (199 + 74 goldens).
- Goldens regenerated for the changed visuals (28 per OS) and reviewed; 3 consecutive Windows runs gave 74/74.
- `flutter analyze` 0 issues; `dart format` no diff; `flutter build appbundle --release` built.
- Each fix re-checked on the Pixel 9a (screenshots and accessibility-tree dumps); see evidence file.
