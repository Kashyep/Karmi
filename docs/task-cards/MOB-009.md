# MOB-009 (T4.5): Tasks

**Status**: Done
**Depends on**: API-001 (`GET /v1/tasks`)
**Files**: `mobile/lib/features/tasks/tasks_screen.dart`, `mobile/lib/shell/karmi_shell.dart`

- [x] Replaced the shell's Tasks placeholder.
- [x] Lists account tasks (open first), each with a `StatusChip` (Open / Completed).
- [x] "Mark complete" → `PATCH /v1/tasks/{id}/complete`; failure keeps the task open and explains.
- [x] Loading, empty, error + Retry, pull to refresh.

Gap: tasks cannot be created from this screen (not requested; tasks are created from Chat).

## Verification
`test/features/screens_test.dart` group "T4.5 Tasks"; a11y guidelines; text scale 2.0; goldens
`tasks_*`.
