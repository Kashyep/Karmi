# MOB-001: Mobile Client Scaffold (Phase 1)

**Status**: Blocked (Flutter toolchain not available in environment)
**Owner**: Unassigned

## Objective
Create the Flutter project scaffold and add initial dependencies and assets as specified in the UI Implementation Plan Phase 1.

## Tasks
- [ ] **T1.1**: Create the Flutter project with `flutter create --org ai.karmi --platforms android,ios mobile` using Flutter 3.47.5 (pinned).
- [ ] **T1.2**: Add pinned dependencies: `liquid_glass_widgets`, `google_fonts`, `cupertino_icons`, `shared_preferences`, `http`.
- [ ] **T1.3**: Bundle Google fonts (Exo, Proza Libre, Trykker) and licenses into `mobile/assets/google_fonts/`, and disable runtime fetching.

## Verification
- `flutter analyze` is clean.
- App runs on Android emulator.
- `flutter pub get` succeeds.
- Font offline bundle verified via widget tests (licences listed).

## Blockers
- **Flutter SDK missing**: The Windows host environment currently lacks the `flutter` command. Installation of the Flutter SDK (target v3.47.5) is required before this card can proceed.
