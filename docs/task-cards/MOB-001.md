# MOB-001: Mobile Client Scaffold (Phase 1)

**Status**: Done (device/simulator runs of the current build NOT RUN: Pixel disconnected, no macOS host)
**Owner**: Boss (integration)
**Plan**: `docs/UI_IMPLEMENTATION_PLAN.md` §3, §7 Phase 1

## Objective
Create the Flutter project scaffold and add initial dependencies and assets as specified in the UI Implementation Plan Phase 1.

## Tasks
- [x] **T1.1**: Flutter project in `mobile/` created with Flutter 3.47.5 (commit `67e478b`). Android + iOS
  only: generated `web/` and `windows/` were removed (MOB-004). Application / bundle ID `ai.karmi.app`.
- [x] **T1.2**: Exact-pinned dependencies: `liquid_glass_widgets 1.6.2`, `google_fonts 8.2.1`,
  `cupertino_icons 1.0.9`, `shared_preferences 2.5.5`, `http 1.6.0`; `pubspec.lock` committed.
- [x] **T1.3**: Fonts bundled in `mobile/assets/google_fonts/` with google_fonts naming (Exo 500/600/700,
  Proza Libre 400/400-italic/600, Trykker 400, Noto Sans Devanagari 400/500/600/700, Noto Serif
  Devanagari 400). Each file was downloaded from the URL google_fonts 8.2.1 itself uses and its
  SHA-256 + length matched the package's expected values. One `<Family>-OFL.txt` per family
  (from github.com/google/fonts). `lib/fonts.dart` sets
  `GoogleFonts.config.allowRuntimeFetching = false` (API confirmed in google_fonts 8.2.1
  `lib/src/google_fonts_config.dart`) and registers each OFL with `LicenseRegistry`. Noto families
  are declared under `flutter: fonts:` so `fontFamilyFallback` resolves them.

## Findings
- google_fonts overwrites `fontFamilyFallback` with its own family name. `KarmiTypography`
  re-applies the Devanagari fallback after each `GoogleFonts.*` call.

## Verification
- `flutter pub get`: PASS. `flutter analyze`: 0 issues.
- `test/fonts_test.dart`: every text style resolves to its bundled family/variant with fetching
  disabled; Devanagari measurably shapes through the Noto fallback; FontManifest declares both Noto
  families; `LicenseRegistry` has one SIL OFL entry per family; `LicensePage` lists all five.
- Android: `flutter build appbundle --release` PASS. On-device run of this build: NOT RUN (Pixel 9a
  disconnected; a first-pass debug build ran on it earlier, see Memory.md).
- iOS simulator: NOT RUN (Windows host).
