# MOB-005 (T4.1): Usage & plan

**Status**: Done
**Files**: `mobile/lib/features/usage/usage_screen.dart`, `mobile/lib/features/plans/plan_catalog.dart`

- [x] Only real `UsageView` values; before/without a server answer every value reads "Unavailable".
- [x] Load failure shows an `ErrorBanner` ("Usage unavailable") with Retry; no raw error text.
- [x] `UsageMeter`: numbers + words ("Available", "Near limit" ≥80%, "Allowance reached") + bar.
- [x] Four read-only `PlanCard`s (Ananta 20, Yanta 100, Trika 250, Part 500 — mirrored from
  `src/daily_agent/plans.py`, labelled "Synthetic development limit") with "Purchases unavailable".

Gap: no plan-catalogue endpoint; `kPlans` must be kept in sync with `plans.py` by hand.

## Verification
`test/features/screens_test.dart` group "T4.1 Usage" (loaded, near/exhausted words, error+retry,
loading) with a11y guidelines; text scale 2.0; goldens `usage_*` (8 variants per OS).
