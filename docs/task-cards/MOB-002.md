# MOB-002: Mobile Client Foundation (Phase 2)

**Status**: Blocked (Depends on MOB-001)
**Owner**: Unassigned

## Objective
Establish the theme, glass treatments, motion settings, and API client for the new Flutter mobile app.

## Tasks
- [ ] **T2.1**: Add tokens in `mobile/lib/theme/karmi_colors.dart` and typography in `karmi_theme.dart`.
- [ ] **T2.2**: Wire up the glass setup in `main.dart` and `lib/theme/karmi_glass.dart`.
- [ ] **T2.3**: Add motion settings `lib/theme/karmi_motion.dart` and reduce-transparency setting.
- [ ] **T2.4**: Implement the API client `lib/api/karmi_api.dart` and models.

## Verification
- Unit test asserts every hex value and recomputes ratios.
- Goldens pass for UI elements.
- Widget tests cover animations on/off and transparency on/off.
- API client mocked tests cover success and failure paths.
