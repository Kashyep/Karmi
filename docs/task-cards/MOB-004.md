# MOB-004: Mobile housekeeping

**Status**: Done
**Owner**: Boss

## Tasks
- [x] Application / bundle ID `ai.karmi.app` as ADR-0002 Q1 decided: Android `namespace` and
  `applicationId` in `android/app/build.gradle.kts`, Kotlin package moved to
  `android/app/src/main/kotlin/ai/karmi/app/MainActivity.kt`, iOS `PRODUCT_BUNDLE_IDENTIFIER`
  (`ai.karmi.app`, tests `ai.karmi.app.RunnerTests`). Display name "Karmi" on both platforms.
- [x] Debug/profile Android manifests allow cleartext HTTP for the local backend; release does not.
- [x] Deleted generated `mobile/web/` and `mobile/windows/` (Android + iOS only); `.metadata` and
  `analysis_options.yaml` no longer reference them.
- [x] `.idea/`, `*.iml`, `.flutter-plugins-dependencies` confirmed ignored (`git check-ignore`,
  `mobile/.gitignore` lines 16/19/30).
- [x] Removed the duplicated "Mobile Client Initialization" section in `Memory.md`; MOB-001/002/003
  status and checkboxes updated to match the code.
- [x] Copied the plan into the repo as `docs/UI_IMPLEMENTATION_PLAN.md` so cards can cite it.

## Verification
- `flutter build appbundle --release` produces the bundle for `ai.karmi.app` (see release evidence).
- iOS bundle ID change: NOT VERIFIED by a build (no macOS host); CI job `mobile-ios-build` covers it.
