# MOB-012 (T5.1): Mobile CI

**Status**: Implemented; CI run NOT VERIFIED (not pushed from this session)
**Files**: `.github/workflows/ci.yml` (shared file, edited serially)

- [x] `mobile-checks` on `ubuntu-latest` (Java 17 + Flutter 3.47.5): pub get, format, analyze,
  `flutter test --exclude-tags golden`, `flutter test --tags golden` (Linux baselines), appbundle.
- [x] `mobile-ios-build` on `macos-latest`: only `flutter build ipa --release --no-codesign`.
- [x] Python `checks` job unchanged (diff touches only the mobile section).

## Verification
Local equivalents pass (see release evidence). The same test suite incl. goldens passed in an
`ubuntu:24.04` container with Flutter 3.47.5. The GitHub Actions run itself has not been observed.
