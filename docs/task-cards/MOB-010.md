# MOB-010 (T4.6): Plan selection (read-only)

**Status**: Done
**Files**: `mobile/lib/features/plans/plan_selection_screen.dart`

- [x] Four comparable `PlanCard`s, current plan marked, "Purchases unavailable"; no purchase
  control (CLAUDE.md: never enable paid checkout).
- [x] Reached from the app-bar overflow menu and from the chat limit-reached banner.

## Verification
`test/features/screens_test.dart` group "T4.6"; `chat_test.dart` limit-reached → plans; goldens
`plans_*`.
