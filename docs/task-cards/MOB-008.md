# MOB-008 (T4.4): Welcome / development sign-in

**Status**: Done (production auth out of scope, ADR-0002 Q3)
**Files**: `mobile/lib/features/welcome/welcome_screen.dart`, `mobile/lib/auth/session.dart`

- [x] "Continue (development sign-in)" calls `POST /dev/token` only; the token is stored in
  SharedPreferences (synthetic dev credential; production needs secure storage).
- [x] States: idle, loading, network error ("Can't reach Karmi") / server refusal with Retry,
  session ended (after any 401), dev auth disabled (`KARMI_DEV_AUTH=false`, default off in release).
- [x] AI/data disclosure copy.

## Verification
`test/features/screens_test.dart` group "T4.4 Welcome"; `chat_test.dart` 401 → Welcome; goldens
`welcome_*`.
