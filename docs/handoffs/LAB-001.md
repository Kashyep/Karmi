# LAB-001 handoff

Task ID and requirements: LAB-001; REQ-LAB-001, REQ-LAB-002; Phase 3 offline ModelLab.

Role and effective model evidence: implementation worker; requested `gpt-5.6-luna/medium`. Effective model/effort metadata was not exposed by the runtime, so identity is unknown. Visible runtime evidence was native Windows Python 3.14.3 and Codex CLI 0.153.4.

Base commit and candidate tree: base `b67c964288e24bf5d3494368766b9c5a7ed25e7f`; candidate commit recorded below after commit.

Files changed:

- `src/model_lab/__init__.py`
- `src/model_lab/core.py`
- `src/model_lab/cli.py`
- `tests/model_lab/conftest.py`
- `tests/model_lab/test_core.py`
- `docs/handoffs/LAB-001.md`

Behavior: added offline suite validation, protected candidate-only export with prompt hashes, JSONL/CSV imported-attempt normalization with unknown metrics preserved as `None` and duplicate/missing rows quarantined, deterministic exact-JSON grading with rubric abstention, and escaped JSON/CSV/Markdown/HTML report rendering. Added Typer commands for suite validation/export, import, grade and report. No paid/provider calls are possible in this slice.

Commands and results (native Windows checkout):

- `python -m pytest tests/model_lab -q` — PASS, 3 passed.
- `PYTHONPATH=src python -m model_lab.cli suite validate benchmarks/seed_cases.jsonl` — PASS, `valid: 60 cases`.
- `PYTHONPATH=src python -m model_lab.cli suite export benchmarks/seed_cases.jsonl --candidate-only --out .tmp-candidates.jsonl` — PASS, 60 records; temporary output removed.
- `PYTHONPATH=src python -m compileall -q src/model_lab` — PASS.

Manual scenarios: inspected the first candidate export; it contains `case_id`, messages, limits, suite version and prompt hash, and excludes `evaluation` and `split`. HTML rendering escapes a malicious `<script>` response.

Commit/recovery: candidate commit is the durable recovery point for all files listed here. Recover with `git show <candidate-commit>` or cherry-pick it into the integration branch.

Known failures/risks: rubric tasks intentionally abstain pending human or calibrated semantic review; this slice does not implement SQLite persistence, fake-provider execution, aggregate imports, charts, Promptfoo adapters, or human-review workflows. CLI command invocation requires the project package to be installed or `PYTHONPATH=src` in an uninstalled checkout.

Next action: Boss should review the exact candidate, integrate serially, and run the broader Phase 3 acceptance suite.

