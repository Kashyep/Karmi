# LAB-002 handoff

Task ID and requirements: LAB-002; Phase 3 offline ModelLab follow-up.

Role and effective model evidence: implementation worker; requested `gpt-5.6-luna/medium`. Effective model/effort metadata was not exposed; identity remains unknown.

Base candidate: current shared `main` after LAB-001/WEB-001 work. Root-owned uncommitted backend files were not staged or modified by this handoff.

Files changed:

- `src/model_lab/core.py`
- `src/model_lab/cli.py`
- `tests/model_lab/test_core.py`
- `docs/handoffs/LAB-002.md`

Behavior added: deterministic `fake_run` with bounded case budget and injected failures; append-only SQLite attempts/grades store with duplicate-attempt rejection and provenance JSON; coverage and missing-case metrics; JSON/CSV/Markdown/standalone HTML report support from imported records plus dependency-free static SVG grade chart; `db-report` CLI; `router recommend` draft output that never writes production configuration.

Commands and results (native Windows checkout):

- `PYTHONPATH=src python -m pytest tests/model_lab -q` — PASS, 5 passed.
- `PYTHONPATH=src python -m model_lab.cli run --suite benchmarks/seed_cases.jsonl --db .tmp-lab.sqlite --budget 5 --fail-every 3` — PASS, 5 attempts, coverage 5/60, one injected failed attempt; temporary database removed.
- `PYTHONPATH=src python -m model_lab.cli db-report --suite benchmarks/seed_cases.jsonl --db .tmp-lab.sqlite --out .tmp-chart.svg --format svg` — PASS; temporary SVG removed.
- `PYTHONPATH=src python -m model_lab.cli router recommend --suite benchmarks/seed_cases.jsonl --db .tmp-lab.sqlite --out .tmp-router.json` — PASS; draft was `eligible: false` with incomplete coverage; temporary JSON removed.
- `PYTHONPATH=src python -m compileall -q src/model_lab` — PASS.
- `ruff check ...` — NOT RUN; `ruff` executable is unavailable in this environment.

Manual observations: fake output is deterministic and contains no network/provider path. SQLite rejects duplicate attempt IDs. Recommendation explicitly reports `draft: true`, `eligible: false` for partial evidence, and states production router was not modified. HTML remains escaped through the existing report renderer.

Known risks: this is still an offline slice; rubric tasks abstain pending human/calibrated semantic review. SQLite is intentionally minimal and does not yet persist suite manifests, run plans, or chart data beyond attempts/grades. Root should review the exact diff because this shared checkout had prior uncommitted edits in the same ModelLab files.

Next action: Boss/Supervisor review the candidate and run the integrated Phase 3 acceptance suite.

